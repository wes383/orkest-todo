# Copy to mobile/.env.local and fill in before building.
# Same values as the root .env.local — the mobile page talks to the same
# Supabase project as the desktop app.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
