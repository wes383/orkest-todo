# Copy to .env.local and fill in before building.
# The anon key is public by Supabase's design — RLS is the real gate —
# this only keeps the project address and key out of git.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
